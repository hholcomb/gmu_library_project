<h1>All Books</h1>
  
  <%= page_entries_info @books %>

  <ol>
    <% @books.each do |book| %>
    <li><%= link_to(book.title, book) %> by <%= book.author %></li>
  <% end %>
  </ol>
<%= paginate @books %>


<%= link_to 'New Book', new_book_path %>
